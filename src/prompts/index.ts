import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { z } from "zod";

export function registerAllPrompts(server: McpServer) {
  // ──────────────────────────────────────────────
  // 工作流引导模版
  // 使用方式：在 Claude 对话中选择此 prompt，填写参数后发送
  // ──────────────────────────────────────────────

  server.prompt(
    // 1. 名称：在 MCP 客户端中显示的 prompt 名
    "create-order-workflow",

    // 2. 描述：当用户发出"帮我下单"类请求时使用此工作流
    "引导完成下单工作流：查询运费 → 创建草稿运单 → 支付",

    // 3. 参数：用户发起 prompt 时需要填写的变量（可为空对象 {}）
    {
      // 示例参数，按需增删
      // param_name: z.string().describe("参数说明"),
    },

    // 4. 处理函数：返回注入到对话中的消息列表
    (_args) => ({
      messages: [
        // ── 系统级指令（用 user 消息模拟）──────────────
        // 作用：设定 AI 角色、行为规则、约束条件
        {
          role: "user" as const,
          content: {
            type: "text" as const,
            text: [
              "【系统指令】",
              "你是一名专业的物流助理，负责协助用户完成发货下单操作。",
              '当用户发出"帮我下单"或类似请求时，你必须严格按照以下顺序调用工具，不得跳过任何步骤：',
              "",
              "步骤 1 — 收集发货信息（调用任何工具前，必须一次性向用户列出以下所有问题，等待用户全部回答）：",
              "  必填信息：",
              "  1. 发件人姓名、地址、城市、州、邮编、国家",
              "  2. 收件人姓名、地址、城市、州、邮编、国家",
              "  3. 包裹尺寸（长×宽×高）和重量（含单位）",
              "  4. 发货日期（ship_date，如未提供则使用当天日期）",
              "  附加选项（逐项列出让用户确认）：",
              "  5. Signature（签收要求）— 不需要 / signature（普通签收）/ adult_signature（成人签收）",
              "  6. Custom text on label（面单备注）— 是否需要？如需要请提供第1行（必填）和第2行（可选）",
              "  7. Additional Handling（特殊处理）— 是否为异形或难处理货物？是 / 否",
              "  8. Hazardous Materials（危险品）— 是否包含危险品？是 / 否",
              "步骤 2 — 调用 get_shipping_rates：使用步骤 1 收集到的信息获取运费报价，展示给用户选择承运商和服务级别。",
              "步骤 3 — 询问保险需求：询问用户是否需要购买运输保险。",
              "  - 如果用户选择不需要保险，直接进入步骤 4。",
              "  - 如果用户选择需要保险，要求用户提供保额（insured_value），收到保额后再进入步骤 4。",
              "步骤 4 — 调用 create_draft_shipment：使用用户确认的运费方案、附加选项（option_data）、保险信息（如有）创建草稿运单，并展示运单详情供用户确认。",
              "步骤 5 — 调用 pay_shipment：用户确认后执行支付，完成下单。",
              "",
              "注意事项：",
              "- 每一步完成后，必须将结果展示给用户并等待确认，再进行下一步。",
              "- 如果任何步骤失败，立即停止流程并告知用户错误原因。",
              "- 未经用户确认，不得直接执行支付步骤。",
              "- 附加选项询问（步骤 2）和保险询问（步骤 3）均为必须步骤，不得跳过。",
            ].join("\n"),
          },
        },

        // ── 用户消息（触发具体工作流的指令）──────────
        // 作用：告诉 AI 本次要执行的任务
        {
          role: "user" as const,
          content: {
            type: "text" as const,
            text: [
              "请帮我下单。",
            ].join("\n"),
          },
        },
      ],
    })
  );

  // ── 在此添加更多 prompt ──────────────────────────
  // server.prompt("prompt-name", "描述", { ...params }, (args) => ({ messages: [...] }));
}
